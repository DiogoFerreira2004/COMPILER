/**
 * Const folding dentro do ramo 'then'
 * 5*6 deve ser dobrado para 30.
 */
class FoldIfMul {
    public int foo() {
        int a;
        if (true) {          // condição constante
            a = 5 * 6;       // → 30
        } else {
            a = 0;
        }
        return a;
    }
}
